/** Min length for field-officer checkout phrase (matches env keyword length). */
const FIELD_OFFICER_CHECKOUT_MIN_LENGTH = 42;
const FIELD_OFFICER_CHECKOUT_MAX_LENGTH = 512;

module.exports = {
  FIELD_OFFICER_CHECKOUT_MIN_LENGTH,
  FIELD_OFFICER_CHECKOUT_MAX_LENGTH,
};
